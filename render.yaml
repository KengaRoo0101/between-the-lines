services:
  - type: web
    name: between-the-lines
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PUBLIC_URL
        value: https://between-the-lines-po4k.onrender.com
      - key: ENFORCE_CANONICAL_HOST
        value: "false"
      - key: LOG_REQUESTS
        value: "true"
